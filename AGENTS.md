# AGENTS.md · 项目协作约束

## Git 提交约束（项目所有者要求，2026-09-06 起）

- **未经项目所有者明确同意，禁止执行任何 `git add` / `git commit` / `git push`。**
- 完成代码改动后的正确流程：跑测试与构建验证 → 汇报改动清单与验证结果 → **停下等所有者决定**是否提交。
- 所有者明确说"提交"或"推送"后，才执行对应的 git 操作；提交信息照常写清楚。
- 网络原因推送失败可以原样重试（不算新提交），但不得借机追加未经确认的改动。

## 项目速览

- 架构、机制、配置与运维说明见 `docs/PROJECT.md`；后端测试：`cd backend && .venv/Scripts/python -m pytest tests -q`；前端构建：`cd frontend && npm run build`。
- 配置只认 `.env` 文件（`app/config.py` 不读系统环境变量），测试用内存库，互不影响运行数据。
