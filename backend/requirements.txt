fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
python-jose[cryptography]>=3.3
bcrypt>=4.0
httpx>=0.27
apscheduler>=3.10
alembic>=1.13         # 结构迁移：migrations/ 目录，启动自动 stamp/upgrade
playwright>=1.45
pytest>=8.0
ruff>=0.8             # lint：错误级 + pyflakes（ruff.toml），CI 与本地同配置
# 数据库驱动：默认 SQLite 免装；生产 MySQL/PostgreSQL 按需使用
psycopg2-binary>=2.9
PyMySQL>=1.1
cryptography>=42.0
pyyaml>=6.0
jsonpath-ng>=1.6        # 高级断言：JSONPath 表达式（$.data.items[0].id）
pillow>=10.0            # 流程截图与基线对比（视觉回归）
mcp>=1.9,<2             # MCP 服务：外部 AI agent 调用平台能力（Streamable HTTP）
agentscope==2.0.7.post1 # Agent Brain（批次 B）：2.x 年轻须锁版本，升级前跑 tests/test_brain_agentscope.py
